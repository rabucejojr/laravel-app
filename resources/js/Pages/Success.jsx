import Modal from "@/Components/Modal";
import React from "react";

export default function Success() {
    return (
        <Modal onClose={{}} show={{}}>
            Success
        </Modal>
    );
}
